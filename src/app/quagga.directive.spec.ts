import { QuaggaDirective } from './quagga.directive';

describe('QuaggaDirective', () => {
  it('should create an instance', () => {
    const directive = new QuaggaDirective();
    expect(directive).toBeTruthy();
  });
});
